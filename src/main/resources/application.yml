#port configuration
server:
  port: 80

#thymeleaf
spring:

  #error
  error:
    include-stacktrace: always

  #resources
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  #mvc
  mvc:
    log-request-details: true

  #http
  http:
    multipart:
      enabled: true

  thymeleaf:
    prefix: /WEB-INF/views/
    suffix: .jsp
  devtools:
    add-properties: true
  #jpa properties
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show_sql: true
    hibernate:
      ddl-auto: none
  #database configuration
  datasource:
    name: election_commission
    url: jdbc:postgresql://127.0.0.1:5432/election_commission
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    initialize: true
  #flyway
  flyway:
    baseline-on-migrate: false

  #web
  web:
    resources:
      static-locations: file:${user.dir}/src/main/webapp/static

logging:
  level:
    springframework:
      jdbc:
        core:
          JdbcTemplate: DEBUG
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor: DEBUG
    web: DEBUG

#flyway
flyway:
  enabled: true

jwt:
  secret: javainusejavainusejavainusejavainusejavainusejavainusejavainusejavainusejavainusejavainusejavainusejavainuse

sms:
  otp:
    time: 5
    dev: 123456

  service:
    url: http://itgesms.goa.gov.in/index.php
    username: ceo_avms
    webToken: 7ca371d49314ba02663cc4914c88798d
    number: +14043417638

users:
  environment: PROD


